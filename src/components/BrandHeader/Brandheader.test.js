import { render } from '@testing-library/react';
import BrandHeader from './BrandHeader';

describe('Render brandheader with logo', () => {
  test('', () => {
    //Arrange
    render(<BrandHeader />);
    //Act
    //Assert
  })
})